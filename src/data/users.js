/**
 * The user repository module
 * @module Data/UserRepo
 */

/**
 * 
 * @param {module:App/User.UserRecord} user - user to save
 * @return {module:App/User.UserRecord} - returns the user saved
 */
function save(user) {

}

module.exports = {
  save
}